/** @type {import('tailwindcss').Config} */
export default {
  // Tailwind CSS v4 uses CSS-based configuration via @theme in index.css
  // This file is kept for compatibility but configuration is in index.css
}
